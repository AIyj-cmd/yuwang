# 工位鱼王开发规范

## 当前阶段

工位鱼王已经进入持续运营与功能深化阶段。当前重点是稳定既有核心闭环，推进体验打磨、社区增长、社交链路完善、内容治理和管理后台运营能力。后续开发应优先保持现有功能稳定，再做有明确产品目标的增量迭代。

已经具备的核心能力：

- 摸鱼记录提交、后端计算 Fish Power Score、SQLite 持久化、结果展示。
- 今日、周榜、月榜、赛季榜、伪装榜、会议榜、传奇榜。
- 用户注册登录、个人主页、累计等级称号、徽章和成就。
- 评论互动、点赞、收藏、投票、分享卡片。
- 社区广场、工会大厅、圈子广场、我的小组。
- 管理后台、人工审核、举报处理、评论管理、用户状态管理、运营配置、敏感词配置和管理员操作日志。
- 基础内容安全策略，包括敏感词、长度限制、匿名化确认、疑似隐私内容审核。
- 简体中文和英文界面切换。

## 产品定位

工位鱼王是一个轻松、荒诞、偏娱乐化的社区网站。用户提交的是匿名化的“摸鱼记录”和办公精神状态，系统按固定规则计算 Fish Power Score，并生成称号、评论、排行榜和成就。

核心边界：

- 这是娱乐社区，不鼓励用户真实违反职场规则。
- 文案可以幽默，但不能鼓励破坏工作秩序、泄露资料或规避管理。
- 不引导用户上传公司机密、个人隐私、员工证件、聊天记录、客户信息、截图或任何未匿名化资料。
- 不收集真实公司名、部门名、客户名、员工身份、真实地理位置等敏感身份信息。

## 技术栈

前端：

- Vue 3
- Vite
- TypeScript
- vue-router
- Pixel UI: `@mmt817/pixel-ui`
- 图标：`lucide-vue-next`

后端：

- Node.js
- Fastify
- SQLite

当前阶段仍保持轻量架构。不要引入微服务、消息队列、Redis、Elasticsearch、云存储或大型权限系统，除非用户明确要求并且已有实际需求。

## 当前信息架构

左侧导航栏是普通用户主要入口，右侧为对应功能区。导航项应保持精简，不重复展示已经合并进其他页面的能力。

当前导航原则：

- “摸鱼记录提交”承载表单、匿名化确认、描述长度和即时内容保护提示。
- “本次得分结果”承载 Fish Power Score、称号、排名、系统评论和互动入口。
- “排行榜”承载全部排行榜切换与昵称筛选，不在左侧导航底部重复展示榜单。
- “个人主页”承载账号资料、等级称号、徽章、成就和近期记录。
- “安全与内容保护”合并安全提示和内容保护策略。
- “社区广场”是全站公共内容广场。
- “工会大厅”是身份归属和赛季竞争。
- “圈子广场”是主题兴趣聚合。
- “我的小组”是小范围熟人或邀请码空间。
- 管理后台不暴露给普通导航；前台入口只允许当前用户为 admin 时显示。

左侧导航底部统计数据和快捷操作必须保持不重叠。新增入口时要先评估导航高度和移动端布局。

## 管理后台

管理后台统一在 `/admin` 下，和普通前台布局分离：

- `/admin/login` 使用独立登录布局。
- `/admin` 重定向到 `/admin/dashboard`。
- `/admin/dashboard`、`/admin/records`、`/admin/records/:id`、`/admin/reports`、`/admin/comments`、`/admin/users`、`/admin/guilds`、`/admin/circles`、`/admin/groups`、`/admin/safety`、`/admin/settings`、`/admin/audit-logs` 使用 `AdminLayout`。

后台访问保护：

- 所有 `/admin` 页面除 `/admin/login` 外必须先调用 `/api/admin/auth/me` 验证。
- 所有 `/api/admin` 接口必须由后端验证管理员 httpOnly cookie。
- 管理员 token 不得存入 `localStorage`。
- 普通用户 token 不能作为后台权限。
- 管理员密码不能硬编码，必须通过环境变量提供。

管理员环境变量：

- `ADMIN_USERNAME`
- `ADMIN_PASSWORD_HASH`
- `ADMIN_SESSION_SECRET`

生成密码哈希：

```bash
npm run admin:hash-password -- your-password
```

管理后台第一版能力：

- 数据概览：站点统计、最新待审核记录、最新举报、最近管理员操作。
- 记录管理：列表筛选、详情、批准、隐藏、驳回、恢复、审核备注。
- 举报处理：状态流转、无效举报、联动隐藏目标内容。
- 评论管理：搜索、状态筛选、隐藏、恢复、删除。
- 用户管理：轻量账号状态管理，支持 active、muted、banned。
- 工会、圈子、小组管理：启用、停用、隐藏，不收集真实公司部门信息。
- 安全配置：敏感词、敏感等级、近期命中内容、安全提示。
- 站点配置：社区、评论、小组创建、传奇提名、长度限制、默认内容状态和安全提示。
- 操作日志：所有后台写操作必须写入 `admin_audit_logs`。

## 核心业务规则

### 记录提交表单

表单保持轻量，核心字段为：

- 昵称或账号显示名
- 摸鱼类型
- 持续时间
- 风险场景
- 伪装方式
- 创意等级
- 创意描述
- 匿名化确认

不提供图片、截图或文件上传。

### Fish Power Score

公式保持固定：

```text
Fish Power Score =
摸鱼类型基础分 * 持续时间倍率 * 风险倍率
+ 伪装加分
+ 创意加分
```

分数保留 1 位小数。客户端可以展示规则，但最终分数必须由后端根据枚举值计算。不要信任客户端提交的分数。

评分规则、称号等级、排行榜类型、敏感词、徽章定义等集中维护在 `shared/scoring.ts`，不要把规则散落到多个组件里。

### 等级称号

等级称号基于账号或昵称累计 Fish Power Score。个人主页已经集成等级、徽章和成就展示，后续不要再在左侧导航中单独增加“等级称号”或“徽章”入口。

### 排行榜

当前排行榜包括：

- 今日摸鱼王
- 本周带薪摸鱼榜
- 月赛摸鱼榜
- 赛季鱼王榜
- 伪装大师榜
- 会议摸鱼榜
- 传奇摸鱼王榜

排行榜切换和筛选应在排行榜页面内部完成，不再拆散到左侧底部。

### 社区互动

已支持点赞、收藏、投票、评论、举报、传奇提名和分享卡片。新增互动能力时必须考虑：

- 是否需要登录。
- 是否会改变排行榜或徽章。
- 是否需要进入审核。
- 是否需要刷新个人主页和排行榜数据。

### 社交链路

社交链路按四类入口维护，不要混成一个泛社区页面：

- 社区：公共内容流、筛选、点赞、评论、举报、传奇提名。
- 工会：单工会身份归属、工会贡献、工会排行榜和工会任务。
- 圈子：可加入多个主题圈子，新记录按规则自动归类。
- 小组：公开小组和邀请码小组，小组内记录流、挑战和成员空间。

提交记录时必须保留发布范围：

- 默认发布到社区广场。
- 默认自动加入相关圈子。
- 用户加入小组后可以同步到一个或多个小组。
- 仅自己可见与社区广场互斥。
- 如果用户加入工会，记录自动贡献工会积分。

## 审核和安全

安全提示必须持续可见或可到达：

```text
请不要提交公司机密、个人隐私、员工证件、聊天记录、客户资料或未匿名化截图。
本平台仅供娱乐，不支持真实违反职场规则的行为。
```

内容保护至少包括：

- 创意描述长度限制。
- 明显敏感词拦截。
- 手机号、邮箱、链接、疑似公司全称等内容进入审核。
- 匿名化确认必选。
- 后端重新计算分数。
- 后台可以隐藏、驳回、恢复内容，但不能引导管理员收集更多真实隐私。

内容审核要优先保守。任何疑似公司机密、隐私、证件、客户资料、聊天记录或截图内容都不能直接公开。

## 数据库原则

- SQLite 结构变更必须幂等。
- 已存在字段不要重复添加导致报错。
- 不要清空现有数据。
- 不要改坏现有排行榜和提交记录功能。
- 记录、评论、举报、用户、工会、圈子、小组的状态字段必须兼容旧数据。
- 管理后台写操作必须写 `admin_audit_logs`。

## 开发原则

- 保持功能可用优先，避免为小需求引入重型依赖。
- 每个导航入口应只展示对应功能，避免所有内容堆在一个页面。
- 合并同类功能，减少左侧导航重复项。
- 前端布局要像社区工具，不做营销落地页。
- 第一屏直接展示可操作产品。
- 响应式布局必须避免文字和控件重叠。
- 数据写入、互动、审核和排行榜刷新要保持前后端状态一致。
- 不要破坏已有用户数据，SQLite 结构变更要用兼容迁移。
- 修改共享规则或接口类型时，同步更新前端类型、后端路由和相关验证。

## 验证要求

每完成一个功能点都要跑验证，再进入下一个功能点。

基础验证命令：

```bash
npm run typecheck
npm run build
```

本地运行检查：

- API 健康检查：`GET /api/health`
- 配置检查：`GET /api/options`
- 排行榜检查：`GET /api/leaderboards`
- 有登录态功能时检查：`GET /api/auth/me`
- 管理后台认证检查：`GET /api/admin/auth/me`
- 管理后台概览检查：`GET /api/admin/dashboard/summary`

前端改动必须至少验证：

- 页面能正常加载。
- 左侧导航切换后只展示对应功能区。
- 移动端或窄屏下文字、按钮、底部统计不重叠。
- 涉及账号、个人主页、互动或审核时，登录态和未登录态都要检查。
- 管理后台不能由未登录管理员访问。

## 暂缓事项

除非用户明确要求，继续暂缓：

- 付费功能。
- AI 自动审核。
- WebSocket 实时排行榜。
- 复杂权限系统。
- 云存储和文件上传。
- 真实公司、客户、地理位置或身份信息采集。
- 微服务拆分和消息队列。

## Command Output

Protect context usage. **Any command with unknown or potentially large output must be byte-capped.**

Default pattern:

```bash
COMMAND 2>&1 | head -c 4000
```
